class trfRandomStats {
    // input functions to be able to set values in real time
    //   store in an array of bit values
    // functions to find correlations between randomly-selected pairs of
    //   variables
    // functions to find correlations between fabricated things
    //   meaning find f(x) and g(y) where f and g are each one of the 16
    //   binary boolean operators
}
